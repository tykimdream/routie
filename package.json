{
  "name": "routie",
  "version": "0.1.0",
  "private": true,
  "description": "Smart travel route optimizer — helps travelers make better decisions",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "prisma": "^6.5.0",
    "turbo": "^2.4.4"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "prisma",
      "@prisma/engines",
      "@prisma/client",
      "@nestjs/core",
      "sharp",
      "unrs-resolver",
      "bcrypt"
    ]
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  }
}
